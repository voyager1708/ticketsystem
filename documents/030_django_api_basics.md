# Step 3: Django APIの型を学ぶ（15分）

実行ステップは各 `010_StepN_*.md` を参照。

- URLルーティングの基本

　ticket_system/urls.py

- View（関数/クラス）の基本
- レスポンスの返し方
- Swagger上でのリクエスト/レスポンス確認

  ticket_service/views.py の　各get/post と @extend_schema

  ※swagger: 利用を効率化するオープンソースのツールセット
             APIが簡単に使えるようになる

